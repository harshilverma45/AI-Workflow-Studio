# Phase 1 Plan

- Create a minimal execution engine endpoint.
- Add SQLite persistence for executions and iterations.
- Wire the loop service into the API route.
- Prepare a WebSocket stream for live updates.
